<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.mapper.UserMapper">


    <select id="selectAll" resultType="com.example.entity.User">
        select * from user
        <where>
            <if test="username != null and name != ''">
                AND (username LIKE concat('%', #{username}, '%') OR name LIKE concat('%', #{name}, '%'))
            </if>
        </where>
        order by id desc
    </select>

    <insert id="insert" parameterType="com.example.entity.User" useGeneratedKeys="true">
        insert into user(id, username, password, name, avatar, role, sex, phoneNumber, email)
        values (#{id}, #{username}, #{password}, #{name}, #{avatar}, #{role}, #{sex}, #{phoneNumber}, #{email})
    </insert>

    <update id="updateById" parameterType="com.example.entity.User">
        update user set username = #{username}, password = #{password}, name = #{name}, avatar = #{avatar},
        role = #{role}, sex = #{sex}, phoneNumber = #{phoneNumber}, email = #{email}
        where id = #{id} 
    </update>

</mapper>